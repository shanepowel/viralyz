# Moved

The product app is now standalone at **[`/app.viralyz.com`](../../app.viralyz.com)**.

```bash
cd app.viralyz.com
pnpm install
pnpm dev
```

This folder is intentionally empty of application code so the root monorepo no longer builds or installs the product app.
