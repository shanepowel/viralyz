# @repo/database (legacy)

Prisma schema here is **orphaned**. Active ORMs:

- Product tables: `apps/app/shared/schema.ts` (Drizzle)
- Content graph (v2): `packages/db` (Drizzle)

Do not add new models here.
